package com.example.karol.kalkulator_ip.Enums;

public enum Id {
    IP,
    MASK,
    SUBNET,
    BROADCAST,
    FIRST_HOST,
    LAST_HOST,
    CIDR,
    CLASS,
    SUBNET_Q,
    ALL_HOSTS,
    HOSTS_IN_SUBNET,
    GLOBAL_S_SUBNET,
    S_SUBNET,
    S_BROADCAST,
    S_FIRST_H,
    S_LAST_H
}
